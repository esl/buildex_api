<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta name="description" content="">
    <meta name="author" content="">

    <title>Hello Buildex.API.</title>
    <link rel="stylesheet" href="<%= static_path(@conn, "/css/app.css") %>">
  </head>

  <body>
    <div class="container">
      <header class="header">
        <nav role="navigation">
          <ul class="nav nav-pills pull-right">
            <li>
             <div class="btn-group">
              <%= if assigns[:current_user] == nil do  %>
                <%= link "Login", to: auth_path(@conn, :request, "github"), class: "btn btn-primary" %>
              <% else %>
                 <%= link "Logout #{@current_user.username}", to: auth_path(@conn, :delete), method: :delete, class: "btn btn-primary" %>
                 <%= link "See All Repos", to: repositories_path(@conn, :index), class: "btn btn-primary" %>
                 <%= link "Add New Repo", to: repositories_path(@conn, :new), class: "btn btn-primary" %>
              <% end %>
              </div>
            </li>
          </ul>
        </nav>
        <span class="logo"></span>
      </header>

      <p class="alert alert-info" role="alert"><%= get_flash(@conn, :info) %></p>
      <p class="alert alert-danger" role="alert"><%= get_flash(@conn, :error) %></p>

      <main role="main">
        <%= render @view_module, @view_template, assigns %>
      </main>

    </div> <!-- /container -->
    <script src="<%= static_path(@conn, "/js/app.js") %>"></script>
  </body>
</html>
